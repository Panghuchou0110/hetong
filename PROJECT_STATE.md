# hetong 当前核心状态

## 基础
- Node.js + Express + EJS + SQLite + PM2。
- 入口 `app.js`，主页面 `views/index.ejs`，模板 `templates/0113.docx`，无独立 `routes` 目录。
- 代码/数据：`/root/hetong`、`/root/hetong_data`、`/root/hetong_data/orders.sqlite`。
- 本地目录 `D:\hetong`，当前工作区可能是 `D:\HUISHOU\hetong`。
- 中文文件统一 UTF-8。

## 模块与页面
- 模块：登录、合同生成、订单管理、对账表、跑路哥、安全功能。
- 页面：`home`、`runaway`、`contract`、`orders-table`、`orders`、`add`、`safety`。

## 当前 UI
- 主页面 `views/index.ejs` 已升级为现代暗黑科技 SaaS Dashboard 风格。
- 视觉特征：深蓝黑渐变背景、左侧深色导航栏、顶部信息栏、半透明玻璃卡片、大数字 KPI、蓝紫/青绿色按钮高亮、紧凑表格和行 hover 高亮。
- 当前保留原有业务逻辑与数据库结构，本次额外补充了合同生成页的激活字段和更宽松的姓名识别，不影响订单/导出/筛选/预警现有功能。
- 合同生成页“是否激活”只保留 `未激活` 和 `仅激活（直结）` 两项，放在机型后面；激活识别支持全文显式标签与关键词判断，`仅激活/已激活/激活1-5天/激活3天内/国行激活` 优先判为 `仅激活（直结）`，`激活状态/是否激活/激活情况` 后面的值也会参与判断，`未激活/全新未拆封/未拆封/原封/全新原封/未使用/0充` 判为 `未激活`。
- 识别前会先清洗输入文本，去掉零宽字符、全角空格、Tab 和多余空行；姓名只从主文本提取，支持 `姓名/客户姓名/出卖人/名字` 的显式写法，遇到联系人区域关键词后会截断，不再从联系人区域反推姓名。
- 合同生成页识别不到状态关键词时保持默认状态，不乱猜；订单新增页仍保留原有激活选项。
- 新增独立页面 `/iphone-price`，用于 iPhone 17 批发价格表，手机优先深色报价单 UI，支持原始价格保存、清空、复制微信文字版、批次调整，仅影响复制结果不改原价。
- iPhone 价格页已拆成两个入口：`/iphone-price` 为客户截图用紧凑 UI 页，`/iphone-price/text` 为纯文字独立页；主页面不再保留文字备选区，所有控制都下沉到底部。
- `/iphone-price` 默认进入查看模式，保存后切回价格展示；顶部只保留标题、时间和一句提示，适合直接截图给客户。
- 报价页新增 `原价版` 批次选项，自定义减额需要点 `确定` 才生效，复制文字时才会按所选批次扣减，原始输入价格始终不变。
- 文字独立页直接读取同一份保存状态，和主页面保持同步。
- 从报价 UI 页进入文字页时，会先自动同步未保存的编辑价格，并把当前批次减额带到文字页生成报价；文字页仍不显示批次调整按钮。
- 报价页已进一步压缩为更适合手机截图的紧凑版式，去掉了无效占位元素，主视觉更偏报价单而非后台表单。
- iPhone 价格数据持久化到 `data/iphone-prices.json`，固定机型包含 `iPhone 17 Pro Max`、`iPhone 17 Pro`、`iPhone 17`、`iPhone 17 Pro Max 仅激活`；其中 `iPhone 17` 现在支持黑色、白色、青雾蓝、鼠尾草绿、薰衣草紫五种颜色，其余机型保持蓝色/橙色/白色。
- 报价页的批次按钮会直接联动页面展示价和复制文案；只有 `原价版` 才允许编辑、保存和清空价格，非原价模式仅可查看和复制。
- 报价页当前为更大字号、更宽松的手机截图风格；颜色只显示圆点，查看态不再显示输入框底色和边框。
- 报价页颜色圆点右侧已预留短颜色文字（蓝/橙/白/黑/青/绿/紫），底部内部控制区新增“展示颜色文字”复选框，默认勾选；内存胶囊和价格分隔间距已进一步压缩，减少上下占用。
- 报价页新增按机型卡片展示的 `全色联动` 按钮，只在原价编辑态显示，保存后自动隐藏；默认针对多颜色机型开启，输入任意一个颜色价格会同步到同容量的其余颜色，方便日常快速填价，`iPhone 17` 的 5 色也会一起联动。
- 报价页主标题和时间文案已切换为 `北京时间` 口径，标题格式为 `Apple新机专业报价·x月x日`；机型标题已居中显示，`仅激活` 以换行括号、更大字号和特殊颜色突出。
- 报价页右上角新增 `VIP` 角标，仅作视觉标识，不影响原有编辑、保存、复制和联动逻辑。
- 报价页右上角 `VIP` 角标已改为悬浮定位并收紧尺寸，确保单行显示，不再换行。
- 报价页右上角 `SVIP` 已恢复为纯文字带细边框角标，仅作视觉标识，不影响功能。
- 报价页底部控制区已改成默认折叠的 `内部使用 请勿外传` 区块；`文字页` 入口放在底部按钮区，复制文字功能保留在文字页中。
- 文字页现在会从 `/api/iphone-price` 主动刷新最新保存价格，打开旧标签页也会自动同步；浏览器端刷新和复制逻辑已与服务端首屏格式统一，标题、北京时间和三色一行规则保持一致。
- 文字页“微信发送内容”已增加服务端首屏兜底渲染，避免首次打开时内容为空，并继续保留前端自动刷新和复制逻辑。
- 文字页已删除批次调整区，只保留返回 UI 页和复制文字版，文字内容直接展示当前保存的原始价格。
- 文字页已去掉标题日期和前置点号，复制按钮移动到“文字版报价”卡片右上角；卡片内容改为无换行紧凑展示，报价数字统一橙色高亮。
- 文字页报价排版已调整为机型标题更大、每个颜色单独换行展示，便于截图和微信发送。
- 文字页抬头文案已改为 `iPhone17系列新机价格｜北京时间`，页面标题和复制内容统一为新口径。
- 文字页抬头现已改成 `iPhone17系列新机价格` + 单独一行北京时间；颜色排版按每行最多三个颜色展示，超出的颜色自动换到下一行。
- iPhone 报价 UI 页和文字页顶部已删除 `IPHONE 17 SERIES` 英文副标题行。
- `iPhone` 报价页与文字页的页面路由和保存/读取接口已补上 `requireSession + requireAuth`，只能在登录后访问，避免内部报价页被未授权直接打开。
- `iPhone` 报价页与文字页的网页入口已改为未登录直接跳回首页登录页；API 仍保持原有鉴权拦截。
- 报价页自定义减额的“确定”按钮现在会弹出反馈，明确提示当前应用的扣减金额或原价版状态。
- 报价页内存容量标签 `256G / 512G` 已改成带边框的胶囊样式，便于和颜色输入区分层级。
- 报价页内存容量标签已再增强为更明显的渐变高光胶囊样式，层级更清晰。
- 报价页内存标签与颜色输入区间距已微调，排版更干净紧凑。
- 报价页颜色圆点和价格之间已加入横向细内分隔线，颜色价格格子之间也加入竖向细内分隔线，增强截图时的价格格子层级，但不增加外框和底色。
- 报价页顶部“北京时间”徽章颜色已改为与 `SVIP` 更接近的金色系。
- 首页主题切换已恢复双状态，默认未保存时进入真正的日间浅色模式；浅色模式已覆盖页面背景、侧边栏、顶部栏、卡片、筛选输入框、订单卡片、价格行、复选框、弹窗内容、导航主色和表格/跑路哥看板区域，日间模式下统一使用浅色背景和深色文字。
- 首页导航选中态已去掉浅色模式下的白色高光底，改为蓝色左侧强调条和浅蓝选中态。
- 订单管理里的原“订单列表”入口和标题已改名为“卡片订单”；对账表格快捷时间按钮已改为“今日订单/昨日订单”，并按当天/昨天筛选；总表格额外显示“今月订单/上月订单”按钮，用于按本月/上月筛选。
- 订单状态保存已加入 `stateVersion` 乐观锁：前端读取版本并随保存提交，后端发现旧窗口/旧设备版本不一致会返回冲突，前端提示并刷新，避免旧页面覆盖新订单数据。
- 订单数据读取失败时，前端不再用空数据继续启动；登录和自动登录流程会等数据加载成功后才隐藏登录层，避免服务器异常时进入空壳页面或误保存空订单。
- `iPhone` 报价 JSON 读取失败不再静默返回空价格；只有文件不存在时才初始化为空，保存时改为临时文件写入后 rename，降低半写入损坏风险。
- 数据库结构初始化失败时服务不再继续监听端口，而是退出交给 PM2 报错/重启，避免页面能打开但数据不可用。
- 安全功能页已改为折叠面板结构，备份/恢复、操作记录、订单垃圾桶、订单来源管理、账号管理、机型配置、修改密码都可单独展开/折叠。
- 机型配置已并入安全功能页，不再作为左侧独立页面入口；机型管理和颜色管理功能保留，继续同步到合同生成、订单管理和识别。
- 安全功能新增“操作记录”，展示最近 7 天订单/垃圾桶/配置修改记录，记录账号、操作时间、操作类型、纯文本明细；新增订单、删除、恢复、价格变更、批量操作都会记录。
- 操作记录支持对当前最新一次可撤回变更执行撤回，撤回前需要管理员密码；若该记录之后已有新修改，系统会拒绝撤回，避免覆盖后续数据；撤回行为本身也会写入操作记录，并保存撤回前快照，方便误撤回时在无新修改的前提下再撤回这次撤回。
- Excel/ZIP 导出依赖已改成本地 `/vendor/xlsx-js-style` 和 `/vendor/jszip` 加载，不再依赖外部 CDN；`package.json` 已加入 `xlsx-js-style` 与 `jszip` 依赖。
- 主导航和快捷操作已新增 `iPhone价格表` 入口，可直接跳转到报价表页面，不影响合同、订单、结算流程。

## 数据库
- `state(key,value)`：配置 JSON，只存 `sources/defaultSource/models/modelColors/authRememberHours`。
- `defaultSource` 当前默认值为 `皖顺`；来源下拉仍可手动切换到 `成都` 或配置页中的其他来源。
- `orders(id,payload,created_at,hash)`、`trash(id,payload,created_at,hash)`：业务字段都在 `payload`。
- `auth_users(username,salt,hash,created_at)`：页面账号。
- `operation_logs(id,created_at,user,action,summary,detail,undo_state,before_version,after_version,undone_at,undone_by)`：安全功能操作记录，保存最近 7 天可查看记录；`undo_state` 用于撤回当前最新一次变更。
- 不随意改 SQLite 表结构。

## 订单字段
- 基本：`id`(10001+)、`createdAt`、`order_date`。
- 客户：`seller_name/seller_id/seller_phone`。
- 设备：`model/memory/color/actual_model_color/sf_no`。
- 金额：`buy_price`、`settle_price`。
- 分类：`source`、`status`、`settlement`、`remark`、`activation`。
- `status`：`signed`、`group`、`stored`、`stored_mismatch`、`runaway`、`refunded`、`unsigned`。
- `settlement`：`unsettled`、`settled`。
- `activation`：未激活、预激活、仅激活（直结/待补差/已补差）。
- 跑路字段：`runaway_amount`、`runaway_returned_amount`、`runaway_returned_to_me_amount`、`runaway_wanshun_returned_amount`。

## 订单规则
- `unsigned` 自动 `settle_price=0`。
- `refunded` 自动 `settle_price=0`、`settlement=settled`，备注追加“已结算xxx，已退款”。
- `actual_model_color` 与下单型号不一致时转 `stored_mismatch` 并补 `【待补差！】`；清空实际型号可回 `group`。
- 仅激活（待补差）自动补 `【待补差！】`，已有 `【已补差】`/`【待补差！】` 不重复。
- 预警：已签超 3 天未拉群、超 7 天未入库、仅激活待补差、型号不对待补差、已入库未填实际型号；未签合同订单不参与订单预警。

## 跑路哥
- 数据源：`orders` 中 `status==="runaway"`，按当前子入口固定 `source`。
- 金额输入支持数字、`3000+6000`、`3000+6000=9000`，统计取最终值。
- 默认按时间从新到旧排序；快捷筛选：全部/当月/前月/前前月；支持清除筛选后一键恢复所有跑路哥列表；可按时间或金额排序并导出当前筛选 Excel/CSV。
- 跑路哥顶部统计区已拆成可折叠总览模块，表格单独成模块；皖顺和成都都默认展开总览。

### 皖顺模式
- 字段：`settle_price`=已给皖顺结算；`runaway_amount`=客户已拿；`runaway_returned_amount`=客户退皖顺；`runaway_returned_to_me_amount`=客户退我；`runaway_wanshun_returned_amount`=皖顺已退我。
- 公式：客户总已退=客户退皖顺+客户退我；客户还欠=客户已拿-客户总已退；皖顺还差我=已给皖顺结算-客户还欠金额-皖顺已退我。
- 默认：客户退皖顺/客户退我/皖顺已退我未填按 0；客户已拿未填显示未填写。
- 显示：客户总已退 0 显示 `0`；客户还欠 <=0 显示“已结清”；皖顺还差我 >0 直接显示金额，=0 显示“已结清”，<0 显示“皖顺多退我：xxx”（取绝对值）。
- 表格列：时间、来源、客户、手机、已结算金额、跑路金额、客户退皖顺、客户退我、客户总退（自动）、客户还欠（自动）、皖顺已退我、皖顺还差我（自动）、备注。
- 可编辑：备注、客户已拿、客户退皖顺、客户退我、皖顺已退我、结算价。
- 皖顺视图顶部已加入三张风险卡和两块轻量图表，优先突出皖顺还没退回金额、总共跑路金额、客户还没要回金额。

### 成都模式
- 字段：`runaway_amount`=已转客户/客户已拿；`runaway_returned_amount`=客户已退。
- 公式：客户还欠=客户已拿-客户已退；还欠 >0 显示“追回中”，否则“已结清”。
- 默认：客户已拿、客户已退未填按 0。
- 表格列：时间、来源、客户、手机、客户已拿、客户已退、客户还欠、状态、备注。
- 跑路哥-成都顶部看板已改成当前筛选统计的“总共跑路金额 / 已退回金额 / 当前还欠金额”，并配有跑路金额对比和退回进度图，不再显示额外的三张小统计卡，也不单列当月卡片。
- 对账表筛成都时显示“已转客户金额”，写回同一个 `runaway_amount`。

## 导出
- 前端 `xlsx-js-style` + CSV(BOM)。
- 导出文件名统一包含日期或日期范围，并标明导出内容类型与范围/筛选，例如全部订单备份、分时段订单备份、对账表格当前筛选、批量导出、跑路哥当前筛选、垃圾桶备份、导入模板备份、买卖合同。
- 安全功能：全部订单 Excel、分时段 Excel、订单 CSV、垃圾桶 CSV、导入模板 CSV。
- 安全功能新增“一键导出备份”按钮，前端直接打包生成 `备份_YYYY-MM-DD.zip`，zip 内包含 3 个 Excel：`订单备份_全部订单_皖顺成都.xlsx`、`跑路订单_皖顺.xlsx`、`跑路订单_成都.xlsx`。
- 一键导出备份使用现有订单数据字段，金额字段保持为数字，日期字段使用可读格式；不改数据库结构、不影响现有导出按钮。
- 对账表：当前筛选导出；成都相关视图额外导出“已转客户金额”；表格默认隐藏选择框，点击“批量操作”后才显示逐行勾选、全选当前表格和批量工具栏，并复用订单列表的批量结算、批量填结算价、生成结算账单、批量导出、批量已入库、批量拉群、批量删除功能。
- 批量填结算价保留“按客户填价”逐单填写；新增“按机型填价”，会按机型、容量、颜色、激活类型分组后一次填写同组价格。
- 批量导出：选中/当前筛选/全部，Excel/CSV 可选。
- 跑路哥导出：皖顺/成都列不同，导出当前计算结果。
- Excel 样式：自动列宽、表头深色、行高、状态/激活/结算颜色；“已补差”优先绿色。

## 开发规则
- 默认只读 `AGENTS.md` 和本文件，不读 `PROJECT_HISTORY.md`。
- 只有明确查历史、提到历史日期、本文件无法解释当前逻辑、或需要恢复旧设计时才读 `PROJECT_HISTORY.md`。
- `views/index.ejs` 只能局部改，不整页重写。
- 不删除旧功能，不大规模重构，不随意改数据库结构，不随意改合同模板。
- 功能变更后优先更新本文件，只写当前最终状态，不写演变过程。
