先读取 AGENTS.md 和 PROJECT_STATE.md，使用 UTF-8。

本次只修改我接下来要求的内容。
不要重构。
不要删除旧功能。
不要分析整个项目。
只读取与本次需求相关的文件。

修改前先告诉我：
1. 准备修改哪些文件
2. 是否会影响现有功能
3. 是否需要修改数据库

修改完成后：
更新 PROJECT_STATE.md。
默认不要读取 PROJECT_HISTORY.md。
===============
用5.4 mini
==========
备份 命令
git status
git add .
git commit -m "备份名字"
git push origin master
==========
服务器里
cd /root/hetong
git pull origin master
npm install
pm2 restart hetong
pm2 status
